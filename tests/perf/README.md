# Performance Harness

This directory is for tests that measure performance characteristics once semantics are stable enough to compare meaningfully.

Likely contents later:

- benchmark scenario definitions
- batch shape and data distribution fixtures
- runtime metric capture formats

Current rule: do not optimize or benchmark placeholder implementations.
