# Plans

`plans/` tracks execution, not architecture.

- `active/`: current plans being worked
- `completed/`: finished plans kept for history and decision context

Plan files should stay practical:

- state scope
- define phases
- list exit criteria
- note blocked or unresolved items

If a plan changes architecture or semantics, update `docs/` as well.
